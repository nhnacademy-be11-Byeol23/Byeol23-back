server:
  port: 10333

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  data:
    redis:
      host: ${REDIS_HOST:s4.java21.net}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:*N2vya7H@muDTwdNMR!}
      database: ${REDIS_DATABASE:321}
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  quartz:
    job-store-type: memory
    properties:
      org:
        quartz:
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 4
  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  public-key: dummyData

#eureka:
#  client:
#    enabled: false